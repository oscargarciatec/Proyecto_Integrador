docker volume create alloydb-data

docker run --name spin-voyager \
  -e POSTGRES_PASSWORD= \
  -p 5432:5432 \
  -v alloydb-data:/var/lib/postgresql/data \
  -d google/alloydbomni

docker exec -it spin-voyager psql -U postgres